import { makeStyles } from "@material-ui/core/styles";
import background from "../../images/background.png";

export default makeStyles(theme => ({
  daftarBlock: {
    backgroundColor: "#A828F7",
    height: "100vh",
    backgroundImage: `url(${background})`
  },
}));
